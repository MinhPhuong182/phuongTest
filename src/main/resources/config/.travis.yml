deploy:
    provider: pages
    skip-cleanup: true
    github-token: buildtest123  # Set in the settings page of your repository, as a secure variable
    keep-history: true
    on:
        branch: master
